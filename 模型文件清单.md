# 需单独转发的文件夹清单

GitHub / 代码仓库**不含**下列大文件（超过 GitHub 单文件 100MB 限制）。  
对方拉完代码、跑完 `bash setup.sh` 后，还要把下面目录**原样拷贝**到项目根目录（与 `GPT_SoVITS`、`README.md` 同级）。

路径必须保持一致，不要改名、不要多套一层文件夹。

---

## 必需（不拷贝无法启动）

| 文件夹 | 约大小 | 说明 |
|--------|--------|------|
| `GPT_SoVITS/pretrained_models/` | ~1.4G | 预训练底座（hubert / roberta / s1 / s2 等） |
| `GPT_weights_v2/` | ~0.9G | 微调后的 GPT 模型（`.ckpt`） |
| `SoVITS_weights_v2/` | ~1.0G | 微调后的 SoVITS 模型（`.pth`） |

合计约 **3.3G**。

### 目录内应有内容（核对用）

**`GPT_SoVITS/pretrained_models/`**
- `chinese-hubert-base/`
- `chinese-roberta-wwm-ext-large/`
- `gsv-v2final-pretrained/`
- `s1bert25hz-2kh-longer-epoch=68e-step=50232.ckpt`
- `s2G488k.pth` / `s2D488k.pth`

**`GPT_weights_v2/`**（示例，以你实际训练的为准）
- `01-aiyinbala-e15.ckpt` 等 `.ckpt` 文件

**`SoVITS_weights_v2/`**（示例）
- `01-aiyinbala_e8_s360.pth` 等 `.pth` 文件

---

## 强烈建议（中文效果更好）

| 文件夹 | 约大小 | 说明 |
|--------|--------|------|
| `GPT_SoVITS/text/G2PWModel/` | ~608M | 中文多音字读音模型（含 `g2pW.onnx`） |

缺少时一般仍能启动，但中文多音字可能不准确。

---

## 不必转发（推理用不到）

| 文件夹 | 说明 |
|--------|------|
| `DATA-physics/` | 训练用原始音频，推理不需要 |
| `output/` | 本机生成结果 |
| `venv/` | 虚拟环境，对方用 `setup.sh` 自己装 |
| `__pycache__/`、`.DS_Store` | 缓存/系统文件 |

---

## 推荐拷贝步骤

### 1. 你这边打包（在项目根目录执行）

```bash
# macOS / Linux — 打成几个压缩包，方便网盘分卷上传
tar -czf pretrained_models.tar.gz -C GPT_SoVITS pretrained_models
tar -czf G2PWModel.tar.gz -C GPT_SoVITS/text G2PWModel
tar -czf GPT_weights_v2.tar.gz GPT_weights_v2
tar -czf SoVITS_weights_v2.tar.gz SoVITS_weights_v2
```

或用 U 盘直接复制这 4 个文件夹。

### 2. 对方解压 / 放置位置

假设对方项目在 `~/Desktop/inference_only/`：

```text
inference_only/
├── GPT_SoVITS/
│   ├── pretrained_models/     ← 放这里
│   └── text/
│       └── G2PWModel/         ← 放这里
├── GPT_weights_v2/            ← 放这里
├── SoVITS_weights_v2/         ← 放这里
├── setup.sh
├── start_inference.sh
└── ...
```

解压示例：

```bash
cd /path/to/inference_only
tar -xzf pretrained_models.tar.gz -C GPT_SoVITS
tar -xzf G2PWModel.tar.gz -C GPT_SoVITS/text
tar -xzf GPT_weights_v2.tar.gz
tar -xzf SoVITS_weights_v2.tar.gz
```

### 3. 对方验证并启动

```bash
source venv/bin/activate   # 若已 setup 过
python3 check_env.py
./start_inference.sh
```

`check_env.py` 里模型权重相关项都应显示 ✓。

---

## 对方完整上手顺序（简版）

1. 拿到代码仓库（Git clone 或 zip）
2. `bash setup.sh`（只装 Python 依赖）
3. 向你索取本文清单中的文件夹并按路径放好
4. `./start_inference.sh`

---

## 备注

- 这些文件已在 `.gitignore` 中忽略，**不要**强行 `git add` 进 GitHub。
- 若只转发微调音色、对方已有官方 `pretrained_models`，至少仍需：`GPT_weights_v2/` + `SoVITS_weights_v2/`（以及建议的 `G2PWModel/`）。
- 参考音频一般由对方在 WebUI 里自己上传；`DATA-physics/` 不是启动必需。
